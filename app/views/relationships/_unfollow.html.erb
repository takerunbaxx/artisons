    <%= form_with(model: current_user.relationships.find_by(follow_id: object.id), method: :delete) do |f| %>
      <%= hidden_field_tag :follow_id, object.id %>
      <%= f.submit 'Unfollow', class: "btn btn-outline-secondary" %>
    <% end %>