      <%=form_with(model: user, local: true) do |f|%>
      <% if user.image? %>
        <%= image_tag user.image.thumb50.url, class: "image-style-user-else" %>
      <% else %>
        <%= image_tag "/assets/default.jpg", class: "image-style-user-else" %>
      <% end %>
    <% end %>